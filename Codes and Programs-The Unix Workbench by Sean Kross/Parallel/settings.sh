#!/usr/bin/env bash
echo -e "parallel --max-line-length-allowed\n"
parallel --max-line-length-allowed
echo -e "parallel --number-of-cpus\n"
parallel --number-of-cpus
echo -e "parallel --number-of-cores\n"
parallel --number-of-cores
